# Overview

This repository will hold exercises aiming at teaching the various data types of Python.

# General Rules
- Corrections will run on Ubuntu 20.04 LTS.
- Python version used for correction: Python 3.8.x.
- Every Python file must start exactly with:  
  `#!/usr/bin/env python3`
- Every Python file must:
  * Be executable.
  * End with a newline.
  * Be PEP8 compliant (pycodestyle 2.7.x).
  * Output must match expected formatting exactly.
  * No external libraries are allowed unless explicitly requested.

# Exercises

| Task name                                     | Filename                        |
|-----------------------------------------------|---------------------------------|
| 0. Print a list of integers                   | print_list_integer.py           |
| 1. Safe access to a list element              | element_at.py                   |
| 2. Replace an element in a list               | replace_in_list.py              |
| 3. Print a matrix of integers                 | print_matrix_integer.py         |
| 4. Tuple addition                             | add_tuple.py                    |
| 5. Common elements in two sets                | common_elements.py              |
| 6. Update or add a key/value in a dictionary  | update_dictionary.py            |
| 7. Best score                                 | best_score.py                   |
